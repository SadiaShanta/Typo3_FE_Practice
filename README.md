Echoes package:
(latest project)
I have created a Frame class to add styling. I have added them from the TYPO3 backend in the Appearance tab → Frame field.
Added TypoScript for two navigations: Main Navigation and Footer Navigation.
Using Mask, I created a mask content named ArtistGrid.
For buttons, I have added Button Primary or other variants from the TYPO3 backend and styled them.
For multiple columns, I have added classes from the TYPO3 backend so that I can use them for styling.

Matrix Site Package: 
(old project)
In this project, most of the content elements are Mask content. 
Since the default extension was set to this package, when creating Mask content for other packages, they were automatically placed in matrix_site_package. 
Additionally, the project includes some simple animations to enhance the user experience
